{
  "name": "claw3d-gateway-adapter",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal WebSocket adapter for exposing an OpenClaw gateway behind a simple token gate.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.3"
  }
}
